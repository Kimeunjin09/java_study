public class Ex5_8 {
	public static void main (String[] args) {
	
		// 커맨드 라인을 통해 입력받기
		// 커맨드 라인에 입력한 값이 문자열 배열에 담겨서 전달된다.
		
		// 공백을 구분자로 입력한게 문자열로 간주되며 문자 배열에 들어가게되는것.
		// 이 문자열은 참조변수 args에 들어가게됨. 문자열배열로 바껴서 메인메서드로 넘겨짐
		// 배열의 길이가 0인것도 가능! , ""빈 문자열 == 길이가0인배열 사용하는것처럼
		
		// 예제
		// 방법1
		// 메뉴 run,이나 실행버튼옆 화살표에 Run Configurations -> Arguments -> Program arguments
		// 칸에 스캐너에 입력하는것처럼 원하는 문자열을 입력하면 됨
		
		// 방법2
		// cmd창이용(여기서 클래스파일로 경로이동해야함)
		// 이클립스에 alt + enter 하면 소스파일의 위치가 나옴, bin폴더에 클래스파일있음
		// bin폴더에서 경로 복사해서 cmd창에 cd 경로복사하기 한 후 엔터 -> dir로 클래스 조회후
		// 원하는 클래스를 실행(java + 클래스이름 친 후 엔터하면 됨)
		// 입력 받으려면 클래스이름 뒤에 원하는 문자열 입력하기
		
		System.out.println("매개변수의 개수:"  + args.length);
		for(int i = 0; i < args.length; i++) {
			System.out.println("args[" + i + "] = \"" + args[i] + "\"");
		}
		
		
	}
}
